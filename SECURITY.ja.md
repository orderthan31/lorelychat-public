# セキュリティポリシー

[한국어](SECURITY.ko.md) | [English](SECURITY.md) | [日本語](SECURITY.ja.md)

## サポート対象

リポジトリ公開後は、デフォルトブランチの最新コミットを対象にセキュリティ修正を提供します。

## 脆弱性の報告

認証情報、非公開の会話、キャラクターカード、データベース、脆弱性の悪用に関する詳細を公開イシューに投稿しないでください。

リポジトリで GitHub Private Vulnerability Reporting が有効な場合は、そのチャネルを使用してください。利用できない場合は、GitHub プロフィールからリポジトリ所有者に非公開で連絡し、安全な報告チャネルの案内を待ってください。

次の情報を含めてください。

- 影響を受けるバージョンまたはコミット
- デプロイ構成
- 最小限の再現手順
- 期待する動作と実際の動作
- 機密情報を除去したログ

## デプロイ時の警告

Lorechat は、信頼できる単一ユーザーによるセルフホスティングを前提に設計されています。現在、公開インターネット向けの完全な認証境界は提供していません。

- Compose の既定のバインドアドレスは `127.0.0.1` です。
- 認証済み TLS リバースプロキシまたはプライベート VPN でアクセスを制御しない限り、バインドアドレスを `0.0.0.0` に変更しないでください。
- API コンテナは直接公開されず、すべてのブラウザートラフィックは Web コンテナを経由します。
- プロバイダー key、SQLite データベース、アップロード、プロンプト、ログは機密データとして扱ってください。
- アップグレード前に Docker ボリュームをバックアップしてください。

## 機密情報の管理

- `.env`、プロバイダーシークレット JSON、データベース、アップロード、ログ、バックアップをコミットしないでください。
- Git の履歴または公開ログに入ったことがある認証情報は、すぐに変更してください。
- Web UI で保存したプロバイダー認証情報は、ファイルモード `0600` のローカルファイル形式の機密情報ストアに保存されます。専用ボリュームは Lorechat によって暗号化されません。
- 保存データの暗号化が必要な脅威モデルでは、ホストレベルのディスク暗号化とアクセス制御を使用してください。

## コンテナの既定セキュリティ設定

提供される Compose 構成では、非 root プロセス、読み取り専用のルートファイルシステム、Linux ケイパビリティの削除、`no-new-privileges`、プライベートな名前付きボリューム、ヘルスチェックを使用しています。バインドマウント、デバイス、ブラウザーレンダラー、または外向きプロキシを追加する場合は、これらの制御を改めて確認してください。
